<!DOCTYPE html>
<html>
    <head>
      <meta name="google-site-verification" content="XSVYFwLvmBFFMKoZzHszz3G7VxhHJLAQd1kJG27gIo4" />
      <!-- Create page-specific title (full_title is a helper function) -->
      <title><%= full_title( yield(:title) ) %></title>
      <%= stylesheet_link_tag    "application", :media => "all" %>
      <%= javascript_include_tag "application" %>
      <%= csrf_meta_tags %>
      <%= render 'layouts/shim' %>
    </head>
    <body>
        <%# Each page has the header partial %>
        <%= render 'layouts/header' %>

        <div class="container">
            <% flash.each do |key, value| %>
                <%= content_tag(:div, value, class: "alert alert-#{key}") %>
            <% end %>

            <%# Main Page Content %>
            <%= yield %>

        </div>

        <%# Each page has the footer partial %>
        <%= render 'layouts/footer' %>
        <%# debug(params) if Rails.env.development? %>
    </body>
</html>
